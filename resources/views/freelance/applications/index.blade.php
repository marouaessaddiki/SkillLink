<x-workspace-shell role="freelance" title="My applications" eyebrow="Freelancer workspace">
    <div class="mx-auto max-w-6xl">
        <div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="sl-kicker">Your proposals</p><h2 class="mt-2 text-4xl font-bold tracking-tight text-[var(--ink)]">My applications</h2><p class="mt-2 text-[var(--muted)]">Keep track of every offer from first send to final result.</p></div><a href="{{ route('freelance.missions.index') }}" class="sl-button-primary">Find missions <span>→</span></a></div>
        @if(session('success'))<div class="mt-6 rounded-2xl border border-emerald-200 bg-emerald-50 px-5 py-4 text-sm font-semibold text-emerald-800">{{ session('success') }}</div>@endif
        <div class="mt-7 flex gap-2 overflow-x-auto border-b border-[var(--line)] pb-3"><span class="rounded-full bg-blue-50 px-4 py-2 text-xs font-bold text-[var(--blue)]">All applications</span><span class="rounded-full px-4 py-2 text-xs font-bold text-[var(--muted)]">Pending</span><span class="rounded-full px-4 py-2 text-xs font-bold text-[var(--muted)]">Accepted</span><span class="rounded-full px-4 py-2 text-xs font-bold text-[var(--muted)]">Rejected</span></div>
        <div class="mt-6 space-y-5">
            @forelse($applications as $application)
                <article class="sl-panel overflow-hidden"><div class="flex flex-col justify-between gap-4 border-b border-[var(--line)] bg-white p-5 sm:flex-row sm:items-start sm:px-6"><div><p class="text-xs font-bold uppercase tracking-wider text-[var(--blue)]">Mission application</p><h3 class="mt-2 text-xl font-bold text-[var(--ink)]">{{ $application->mission->title }}</h3><p class="mt-1 text-sm text-[var(--muted)]">Mission budget {{ number_format($application->mission->budget, 2) }} MAD · Deadline {{ $application->mission->deadline }}</p></div><span class="w-fit rounded-full px-3 py-1 text-xs font-bold uppercase tracking-wider {{ $application->status === 'accepted' ? 'bg-emerald-50 text-emerald-700' : ($application->status === 'rejected' ? 'bg-red-50 text-red-700' : 'bg-amber-50 text-amber-700') }}">{{ $application->status }}</span></div><div class="p-5 sm:p-6"><div class="grid gap-4 sm:grid-cols-3"><div class="rounded-xl bg-slate-50 p-4"><p class="text-xs font-bold uppercase tracking-wider text-[var(--muted)]">Proposed price</p><p class="mt-2 text-xl font-bold text-[var(--ink)]">{{ number_format($application->proposed_price, 2) }} MAD</p></div><div class="rounded-xl bg-slate-50 p-4"><p class="text-xs font-bold uppercase tracking-wider text-[var(--muted)]">Submitted</p><p class="mt-2 text-sm font-bold text-[var(--ink)]">{{ $application->date_submission?->format('d M Y') ?? $application->created_at->format('d M Y') }}</p></div><div class="rounded-xl bg-slate-50 p-4"><p class="text-xs font-bold uppercase tracking-wider text-[var(--muted)]">Mission state</p><p class="mt-2 text-sm font-bold capitalize text-[var(--ink)]">{{ str_replace('_', ' ', $application->mission->status) }}</p></div></div><div class="mt-5 rounded-2xl border border-[var(--line)] bg-white p-4"><p class="text-xs font-bold uppercase tracking-wider text-[var(--muted)]">Your message</p><p class="mt-2 text-sm leading-6 text-[var(--muted)]">{{ $application->cover_letter }}</p></div><div class="mt-5 flex flex-wrap items-center gap-3">@if($application->status === 'pending')<a href="{{ route('freelance.applications.edit', $application) }}" class="sl-button-secondary">Edit offer</a><form method="POST" action="{{ route('freelance.applications.destroy', $application) }}">@csrf @method('DELETE')<button class="sl-button rounded-xl border border-red-200 bg-red-50 px-4 py-2 text-sm font-bold text-red-700" onclick="return confirm('Delete this offer?')">Delete offer</button></form>@elseif($application->status === 'accepted' && $application->mission->status === 'in_progress')<form method="POST" action="{{ route('freelance.missions.complete', $application->mission) }}">@csrf<button class="sl-button-primary">Mark mission completed <span>→</span></button></form>@elseif($application->mission->status === 'completed')<span class="rounded-full bg-emerald-50 px-3 py-2 text-xs font-bold text-emerald-700">Mission completed</span>@endif</div>@if($application->mission->status === 'completed')<div class="mt-5 border-t border-[var(--line)] pt-5"><p class="text-xs font-bold uppercase tracking-wider text-[var(--muted)]">Review the client</p><form method="POST" action="{{ route('freelance.missions.review-client', $application->mission) }}" class="mt-3 grid gap-3 sm:grid-cols-[180px_1fr_auto]">@csrf<select name="rating" required class="rounded-xl border-[var(--line)] text-sm"><option value="">Rating</option><option value="5">5 - Excellent</option><option value="4">4 - Very good</option><option value="3">3 - Good</option><option value="2">2 - Needs improvement</option><option value="1">1 - Poor</option></select><input name="comment" class="rounded-xl border-[var(--line)] text-sm" placeholder="Share a short review"><button class="sl-button-primary">Submit review</button></form></div>@endif</div></article>
            @empty
                <div class="sl-panel p-14 text-center"><span class="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-blue-50 text-2xl text-[var(--blue)]">↗</span><h3 class="mt-5 text-lg font-bold text-[var(--ink)]">No applications yet</h3><p class="mt-2 text-sm text-[var(--muted)]">Find a mission that fits your strengths and send your first offer.</p><a href="{{ route('freelance.missions.index') }}" class="sl-button-primary mt-5">Discover missions</a></div>
            @endforelse
        </div>
    </div>
</x-workspace-shell>
