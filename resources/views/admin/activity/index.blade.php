<x-admin-shell title="Activity monitoring">
    <div class="mb-7"><p class="text-xs font-bold uppercase tracking-[.2em] text-blue-600">Monitoring</p><h2 class="mt-2 text-3xl font-bold text-[#111a2e]">Platform activity</h2><p class="mt-2 text-slate-500">A clear pulse of what is happening across SkillLink.</p></div>
    <div class="grid gap-6 lg:grid-cols-2"><section class="rounded-2xl border border-slate-200 bg-white p-6"><h3 class="text-xl font-bold text-[#111a2e]">Recent events</h3><div class="mt-6 space-y-5">@forelse($applications as $application)<div class="flex gap-3"><span class="flex h-9 w-9 shrink-0 items-center justify-center rounded-full bg-blue-50 text-blue-600">↗</span><div><p class="text-sm text-slate-700"><strong>{{ $application->freelance->name }}</strong> submitted an offer for <strong>{{ $application->mission->title }}</strong></p><p class="mt-1 text-xs text-slate-400">{{ $application->created_at->diffForHumans() }}</p></div></div>@empty<p class="text-sm text-slate-400">No application activity yet.</p>@endforelse</div></section><section class="rounded-2xl border border-slate-200 bg-white p-6"><h3 class="text-xl font-bold text-[#111a2e]">Mission events</h3><div class="mt-6 space-y-5">@forelse($missions as $mission)<div class="flex gap-3"><span class="flex h-9 w-9 shrink-0 items-center justify-center rounded-full bg-emerald-50 text-emerald-600">▤</span><div><p class="text-sm text-slate-700"><strong>{{ $mission->client->name ?? 'A client' }}</strong> created <strong>{{ $mission->title }}</strong></p><p class="mt-1 text-xs text-slate-400">{{ $mission->created_at->diffForHumans() }}</p></div></div>@empty<p class="text-sm text-slate-400">No mission activity yet.</p>@endforelse</div></section></div>
+</x-admin-shell>
