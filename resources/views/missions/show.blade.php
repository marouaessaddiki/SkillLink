<x-workspace-shell role="client" title="Mission management" eyebrow="My missions">
    <div class="mx-auto max-w-6xl">
        <div class="flex flex-col justify-between gap-5 sm:flex-row sm:items-start"><div><a href="{{ route('missions.index') }}" class="text-sm font-bold text-[var(--muted)] hover:text-[var(--blue)]">← My missions</a><div class="mt-5 flex flex-wrap items-center gap-3"><span class="rounded-full bg-blue-50 px-3 py-1 text-xs font-bold uppercase tracking-wider text-[var(--blue)]">{{ $mission->category?->name ?? 'Open category' }}</span><span class="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold uppercase tracking-wider text-emerald-700">{{ str_replace('_', ' ', $mission->status) }}</span></div><h2 class="mt-4 text-4xl font-bold tracking-tight text-[var(--ink)]">{{ $mission->title }}</h2><p class="mt-3 text-[var(--muted)]">Published {{ $mission->created_at->diffForHumans() }} · {{ $mission->applications_count }} freelancer offers</p></div><div class="flex gap-3"><a href="{{ route('missions.edit', $mission) }}" class="sl-button-secondary">Edit mission</a><a href="{{ route('client.applications.index') }}" class="sl-button-primary">Manage offers →</a></div></div>
        <div class="mt-8 grid gap-8 lg:grid-cols-[1fr_340px] lg:items-start"><main><section class="sl-panel p-6 sm:p-8"><p class="sl-kicker">About this mission</p><p class="mt-4 whitespace-pre-line text-base leading-8 text-[var(--muted)]">{{ $mission->description }}</p></section><section class="mt-6 sl-panel p-6 sm:p-8"><div class="flex items-center justify-between"><div><p class="sl-kicker">Offers panel</p><h3 class="mt-1 text-2xl font-bold text-[var(--ink)]">Freelancers interested</h3></div><span class="rounded-full bg-blue-50 px-3 py-1 text-xs font-bold text-[var(--blue)]">{{ $mission->applications_count }} total</span></div><div class="mt-6 space-y-4">@forelse($mission->applications as $application)<article class="rounded-2xl border {{ $application->status === 'accepted' ? 'border-emerald-300 bg-emerald-50/50' : 'border-[var(--line)] bg-white' }} p-5"><div class="flex flex-col gap-5 sm:flex-row sm:items-start"><div class="flex min-w-0 flex-1 gap-3"><span class="flex h-11 w-11 shrink-0 items-center justify-center rounded-full bg-[var(--ink)] font-bold text-white">{{ strtoupper(substr($application->freelance->name, 0, 1)) }}</span><div class="min-w-0"><div class="flex flex-wrap items-center gap-2"><h4 class="font-bold text-[var(--ink)]">{{ $application->freelance->name }}</h4>@if($application->status === 'accepted')<span class="rounded-full bg-emerald-100 px-2 py-1 text-[10px] font-bold uppercase text-emerald-700">Selected</span>@endif</div><p class="mt-1 text-xs text-[var(--muted)]">Freelancer · {{ $application->freelance->email }}</p></div></div><div class="text-left sm:text-right"><p class="text-xl font-bold text-[var(--ink)]">{{ number_format($application->proposed_price, 2) }} MAD</p><p class="mt-1 text-xs text-[var(--muted)]">{{ $application->date_submission?->format('d M Y') ?? $application->created_at->format('d M Y') }}</p></div></div><p class="mt-4 rounded-xl bg-slate-50 p-4 text-sm leading-6 text-[var(--muted)]">{{ $application->cover_letter }}</p>@if($application->status === 'pending')<div class="mt-4 flex flex-wrap gap-2"><form method="POST" action="{{ route('client.applications.accept', $application) }}">@csrf<button class="sl-button-primary">Accept offer</button></form><form method="POST" action="{{ route('client.applications.reject', $application) }}">@csrf<button class="sl-button rounded-xl border border-red-200 bg-red-50 px-4 py-2 text-sm font-bold text-red-700 hover:bg-red-100">Reject</button></form></div>@else<span class="mt-4 inline-block text-xs font-bold uppercase tracking-wider {{ $application->status === 'rejected' ? 'text-red-600' : 'text-emerald-700' }}">{{ $application->status }}</span>@endif</article>@empty<div class="rounded-2xl bg-slate-50 p-10 text-center"><p class="font-bold text-[var(--ink)]">No offers yet</p><p class="mt-2 text-sm text-[var(--muted)]">Freelancer proposals will appear here as they discover your mission.</p></div>@endforelse</div></section></main><aside class="lg:sticky lg:top-6"><section class="sl-panel overflow-hidden"><div class="bg-[var(--ink)] p-6 text-white"><p class="text-xs font-bold uppercase tracking-[.2em] text-[var(--mint)]">Mission summary</p><p class="mt-3 text-3xl font-bold">{{ number_format($mission->budget, 2) }} MAD</p><p class="mt-2 text-sm text-slate-300">Budget available</p></div><div class="space-y-5 p-6 text-sm"><div><p class="text-xs font-bold uppercase tracking-wider text-[var(--muted)]">Deadline</p><p class="mt-2 font-bold text-[var(--ink)]">{{ $mission->deadline }}</p></div><div><p class="text-xs font-bold uppercase tracking-wider text-[var(--muted)]">Offers</p><p class="mt-2 font-bold text-[var(--ink)]">{{ $mission->applications_count }} received</p></div><div class="border-t border-[var(--line)] pt-5"><p class="text-xs font-bold uppercase tracking-wider text-[var(--muted)]">Mission timeline</p><div class="mt-4 space-y-4"><p class="font-semibold text-[var(--blue)]">● Mission created</p><p class="font-semibold {{ $mission->applications_count ? 'text-[var(--blue)]' : 'text-slate-300' }}">● Offers received</p><p class="font-semibold {{ in_array($mission->status, ['in_progress', 'completed']) ? 'text-[var(--blue)]' : 'text-slate-300' }}">● Freelancer selected</p><p class="font-semibold {{ $mission->status === 'completed' ? 'text-emerald-600' : 'text-slate-300' }}">● Completed & reviewed</p></div></div></div></section></aside></div>
    </div>
</x-workspace-shell>
